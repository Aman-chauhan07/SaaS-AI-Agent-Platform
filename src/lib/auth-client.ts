// Import the helper to create an auth client for React
import { createAuthClient } from "better-auth/react"

// Create and export the auth client instance
// This will be used in your React app (frontend) to handle login, logout, session, etc.
export const authClient = createAuthClient({})
